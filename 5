def quiz_function(size, p):
    counter = 0

    for start in range(size):
        current = start
        while current < size:
            temp = p
            print(temp)
            print(p)
            counter += 1
            current += 1

    return counter 
